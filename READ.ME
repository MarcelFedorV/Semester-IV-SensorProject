To install:

bleak
fastapi
uvicorn
websockets
sqlalchemy
passlib[bcrypt]
itsdangerous
jinja2


Pls run the app (if possible ofc) from now on only using Docker:

>>  docker-compose up --build

And(!!) in another terminal:

>>  python BLE/ble_bridge.py
