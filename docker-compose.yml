services:

  backend:
    build:
      context: ./backend
      dockerfile: backend.Dockerfile
    container_name: backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:////app/data/users.db
      - BLE_BRIDGE_URL=ws://host.docker.internal:8765
    depends_on:
      database:
        condition: service_started
    volumes:
      - db_data:/app/data
    networks:
      - app-network

  test:
    build:
      context: ./backend
      dockerfile: backend.Dockerfile
    environment:
      - DATABASE_URL=sqlite:////app/data/users.db
      - BLE_BRIDGE_URL=ws://host.docker.internal:8765
    depends_on:
      database:
        condition: service_started
    volumes:
      - db_data:/app/data
    entrypoint: ["python", "-m", "unittest", "discover", "-s", "integration_tests", "-v"]
    networks:
      - app-network

  database:
    build:
      context: ./database
      dockerfile: db.Dockerfile
    container_name: database
    volumes:
      - db_data:/app/data
    networks:
      - app-network

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    command: tunnel --protocol http2 --config /etc/cloudflared/config.yml run
    volumes:
      - ./cloudflared/docker-cloudflare-tunnel.yml:/etc/cloudflared/config.yml
      - ./cloudflared/credentials.json:/etc/cloudflared/credentials.json
    depends_on:
      - backend
    networks:
      - app-network

networks:
  app-network:
  
volumes:
  db_data: